<odoo>
    <record id="view_lead_participant_form" model="ir.ui.view">
        <field name="name">lead.participant.form</field>
        <field name="model">lead.participant</field>
        <field name="arch" type="xml">
            <form string="Participant">
                <sheet>
                    <group>
                        <field name="user_id"/>
                        <field name="role"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
